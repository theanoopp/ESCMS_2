<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.equipshare.es_cms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAPzTe94hZ1YYLqHOxglRdUFdboakDKkCM" />

        <activity android:name=".MainActivity" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.StartActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.RegisterActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.MobileAuthActivity" />
        <activity android:name=".activities.NeedEqActivity" />
        <activity android:name=".activities.TempActivity" />
        <activity android:name=".activities.NeedEqSubmitActivity" />
        <activity android:name=".activities.HaveEquipmentActivity" />
        <activity android:name=".activities.QuotationRequestsActivity" />
        <activity android:name=".activities.ViewQuotationActivity"></activity>
    </application>

</manifest>